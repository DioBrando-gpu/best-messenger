# Локально или на Render → Environment
DATABASE_URL=postgresql://user:password@host:5432/dbname

# На Render PostgreSQL URL выдаётся автоматически при подключении базы к Web Service.
# PORT задаёт Render сам.
